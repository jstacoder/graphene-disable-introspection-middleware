from setuptools import setup

setup(
    name='graphene-disable-introspection-middleware',
    version='0.0.1',
    url='https://github.com/jstacoder/graphene-disable-introspection-middleware',
    packages=['graphene_disable_introspection_middleware'],
)
